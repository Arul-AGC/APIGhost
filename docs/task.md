# APIGhost Execution Tasks

- `[/]` Initialize Git repository and project structure
- `[ ]` Setup `pyproject.toml` and install dependencies
- `[ ]` Implement `Spec Parser` (`parser.py`) using `prance`
- `[ ]` Implement `Chain Builder` (`chain_builder.py`) with Dual-Layer Resolution
- `[ ]` Implement `Data Generator` (`generator.py`) with Three-Tier Resolution
- `[ ]` Implement `Network Engine` (`executor.py`) with WAF resilience and LIFO teardown
- `[ ]` Implement `Verdict Engine` (`verdict.py`) with Multi-Signal Jaccard Scoring
- `[ ]` Integrate components into main CLI flow
- `[ ]` Verify against OWASP crAPI
